mixin buttons(size,color,atr,id)       
    button(type="button" id="btn-"+id class="btn btn_size_"+size+" btn_color_"+color+" btn_color_"+color+"_"+atr) BUTTON
        div(class="ripple ripple_"+color)